apiName: app-demo-dev
performance:
  logging:
    enabled:true
#Authorisationproperties
authorized:
  readonlyaccess:
    channels:
#Netbank readwrite -user_managerment_admin is added for test purpose
  readwriteaccess:
    channels:
